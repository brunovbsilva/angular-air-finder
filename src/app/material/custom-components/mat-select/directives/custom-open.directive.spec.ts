import { CustomOpenDirective } from './custom-open.directive';

describe('CustomOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomOpenDirective();
    expect(directive).toBeTruthy();
  });
});
