import { FilterBattlegroundListPipe } from './filter-battleground-list.pipe';

describe('FilterBattlegroundListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBattlegroundListPipe();
    expect(pipe).toBeTruthy();
  });
});
